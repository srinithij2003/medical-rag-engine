# Test Strategy

- Backend unit/API tests: `backend/tests`
- Frontend component tests: `frontend` Jest setup
- Future integration suites:
  - OCR quality regression tests
  - LLM schema adherence tests by model/version
  - End-to-end upload-to-extraction tests in Docker stack
